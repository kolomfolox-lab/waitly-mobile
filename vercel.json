{
  "buildCommand": "npx expo export -p web",
  "installCommand": "npm install --legacy-peer-deps",
  "outputDirectory": "dist",
  "env": {
    "EXPO_PUBLIC_API_BASE_URL": "https://api.moonlauncher.org"
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
